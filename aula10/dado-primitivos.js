// Tipos de dados primitivos: string, number, undefined, null, boolean, symbol
// Exemplo de string
const nome = "GitHub Copilot";

// Exemplo de number
const idade = 25;

// Exemplo de undefined
let endereco;

// Exemplo de null
const telefone = null;

// Exemplo de boolean
const isOnline = true;

// Exemplo de symbol
const id = Symbol("id");

// Tipos de dados por referência: array, object, function
// Exemplo de array
const numeros = [1, 2, 3, 4, 5];

// Exemplo de object
const pessoa = {
  nome: "João",
  idade: 30,
  endereco: "Rua A, 123",
};

// Exemplo de function
function somar(a, b) {
  return a + b;
}
